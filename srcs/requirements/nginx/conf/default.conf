server {
	listen 443 ssl;
	listen [::]:443 ssl;
	# listen 8443 ssl;
	# listen [::]:8443 ssl;

	server_name makoon.42.fr;

	ssl_certificate /etc/nginx/ssl/nginx.crt;
	ssl_certificate_key /etc/nginx/ssl/nginx.key;

	ssl_protocols TLSv1.2 TLSv1.3;

	root /var/www/wordpress;
	index index.php index.html index.htm;

	# precise comment gerer le php a nginx -> renvoie les requetes php sur le port 9000
	location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass wordpress:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

	#location ~ \.php$ {
	#	include snippets/fastcgi-php.congf;
	#	fastcgi_pass wordpress:9000;
	#}

	# demande a nginx de renvoyer nimporte quelle requete qu'on connait pas sur un 404 error
	location / {
		#try_files $uri $uri/ =404; pour les sites statiques
		try_files $uri $uri/ /index.php?$args;
	}
}


#server {
#	listen 80;
#	server_name localhost;

#	root /ver/www/html;

#	index index.php index.html index.htm;

#	location / {
#		try_files $uri $uri/ /index.php?$args;
#	}

#	location ~ \.php$ {
#		include fastcgi_params;
#		fastcgi_pass wordpress:9000;
#		fastcgi_index index.php;
#		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#	}

#	error_log /var/log/nginx/error.log;
#	access_log /var/log/nginx/access.log;
#}